fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9   # the human suggestion form posts as multipart/form-data

# Only needed to run agents on Claude; the Forge runs in simulation without it.
anthropic>=0.40

# Development
pytest>=8.0
httpx>=0.27               # required by fastapi.testclient
